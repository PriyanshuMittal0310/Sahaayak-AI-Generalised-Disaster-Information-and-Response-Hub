version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: sahaayak-db
    environment:
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=dev
      - POSTGRES_DB=crisis
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev -d crisis"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - sahaayak-network

  redis:
    image: redis:6.2-alpine
    container_name: sahaayak-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - sahaayak-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  sahaayak-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sahaayak-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
      - RSS_FEEDS=https://www.gdacs.org/xml/rss.xml,https://www.gdacs.org/xml/rss_earthquake.xml
      - USGS_API_URL=https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/all_day.geojson
      - RATE_LIMIT=100/minute
      - RATE_LIMIT_WINDOW=60
      - CORS_ORIGINS=*
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - ./uploads:/app/uploads
    networks:
      - sahaayak-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  sahaayak-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: sahaayak-frontend
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_ENV=development
      - REACT_APP_API_URL=http://localhost:8000  # Use localhost for browser access
    volumes:
      - ./frontend:/app
      - /app/node_modules
    working_dir: /app
    stdin_open: true
    tty: true
    depends_on:
      - sahaayak-backend
    networks:
      - sahaayak-network
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  uploads:

networks:
  sahaayak-network:
    driver: bridge
